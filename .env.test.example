# Test Database Configuration
DB_HOST=localhost
DB_NAME=ssisdb_test
DB_USERNAME=root
DB_PASSWORD=your_password_here

# Application Configuration
SECRET_KEY=test_secret_key

# Note: Copy this file to .env.test and update the values for your local test environment
# The CI/CD pipeline will use GitHub secrets instead of these values